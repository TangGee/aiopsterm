在linux 上启动后缺少图标，用gpt-image生成
不填写密码 登录还是直接返回 [process exited: 1] 没有要求输入密码。可以用test_hhhh 这台测试
SSH 二次认证 这个弹窗为什么要重试5次，倒计时 如果没必要去掉这个
去掉默认的主机这个分组
A主机的跳板机是B，B有二次认证，这种情况下连A 失败
AI 面板启动加载慢：当前不是严格懒加载，hydrateConfig 启动时会串行加载很多非首屏数据，AI 历史恢复排在后面；后续需要拆分启动加载，当前可见 AI 会话优先恢复，模型/上下文/命令目录后台并发去重，文件/K8s/插件/知识库等改为进入模块后懒加载。
AI 历史会话超大时的渲染/存储性能风险：如果单个或总历史内容达到很大规模（例如 1G），当前全量恢复消息和渲染列表可能明显影响启动、切会话、滚动和内存；后续需要分页/窗口化渲染、按需加载消息、历史文件分片或数据库化存储，并限制单次进入 renderer 的消息体大小。
