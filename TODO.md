AI 面板启动加载慢：已完成第一阶段修复，hydrateConfig 会提前并发恢复 AI 历史、AI todo、上下文和命令目录，文件/K8s/插件/知识库等非首屏目录刷新不再串行阻塞 AI 会话恢复；后续仍需要更彻底的模块级懒加载、后台并发去重，以及文件/K8s/插件/知识库进入模块后再加载。
AI 历史会话超大时的渲染/存储性能风险：如果单个或总历史内容达到很大规模（例如 1G），当前全量恢复消息和渲染列表可能明显影响启动、切会话、滚动和内存；后续需要分页/窗口化渲染、按需加载消息、历史文件分片或数据库化存储，并限制单次进入 renderer 的消息体大小。
AI 对话 Command 的 CDATA 问题：已确认是模型违反提示词/工具契约时的输出格式；后端已 fail closed，遇到 `<![CDATA[...]]>` 包裹的 `<command>` 返回 `AI_COMMAND_CONTRACT_INVALID`，不会再创建可执行命令卡。
