{
  "manifestVersion": 2,
  "id": "aiopsterm.http-cmdb-provider",
  "displayName": "HTTP CMDB Provider",
  "version": "2.0.0",
  "description": "从标准 HTTP JSON 接口同步 SSH 资产。",
  "engines": {
    "aiopsterm": ">=0.1.0"
  },
  "main": "main.cjs",
  "activationEvents": [
    "onStartupFinished"
  ],
  "required": false,
  "iconKey": "cloud",
  "categories": [
    "Assets",
    "CMDB"
  ],
  "readme": "HTTP CMDB Provider 从用户配置的 HTTP JSON 接口获取资产。访问令牌由宿主加密保存，资产校验和持久化由 aiopsterm 后端完成。",
  "functions": [
    {
      "title": "HTTP CMDB 同步",
      "desc": "支持数组或包含 assets 数组的 JSON 响应。"
    },
    {
      "title": "加密认证信息",
      "desc": "Bearer Token 通过插件 secrets 存储。"
    }
  ],
  "contributes": {
    "commands": [
      {
        "id": "aiopsterm.http-cmdb-provider.test",
        "title": "测试 CMDB 连接",
        "description": "请求已配置的 CMDB 地址并返回资产数量。"
      }
    ],
    "assetProviders": [
      {
        "id": "aiopsterm.http-cmdb-provider.assets",
        "name": "HTTP CMDB",
        "description": "从已配置的 HTTP JSON 接口同步资产。",
        "adapter": "runtime",
        "fields": []
      }
    ],
    "configuration": {
      "title": "HTTP CMDB 配置",
      "properties": [
        {
          "key": "endpoint",
          "title": "CMDB 地址",
          "description": "返回资产数组或包含 assets 数组的 HTTP 地址。",
          "type": "text",
          "required": true
        },
        {
          "key": "token",
          "title": "Bearer Token",
          "description": "可选认证令牌，由宿主加密保存。",
          "type": "password"
        },
        {
          "key": "defaultGroup",
          "title": "默认分组",
          "type": "text",
          "defaultValue": "CMDB"
        }
      ]
    }
  }
}
